{
	"files": [
    "./dist"
  ],
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "npm run clean && tsc",
		"clean": "rimraf ./dist"
	}
	,
	"devDependencies": {
		"typescript": "^5.9.3",
		"rimraf": "^6.1.3",
		"tsc-alias": "^1.8.16"
	}
}
